$kind: http-request
name: "Get patient's appointments"
method: GET
url: '{{baseUrl}}/api/v1/appointment/my-appointments'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
